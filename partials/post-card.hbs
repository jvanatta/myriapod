<article class="post-card {{post_class}}">
    <div class="post-content">
        <header class="post-full-header">
            <h1 class="post-title">
                <a class="post-card-content-link" href="{{url}}">{{title}}</a>
            </h1>
        </header>

        <div class="post-card-byline-content">
            <span class="post-card-byline-date">{{authors}} &mdash; <time>
                <datetime="{{date format="YYYY-MM-DD"}}">{{date format="D MMM YYYY"}}</time>
                        {{#primary_tag}} &mdash; <a href="{{url}}">{{name}}</a>{{/primary_tag}}
                </span>
        </div>

        <section class="post-content">
                {{content}}
        </section>

        <footer class="post-card-meta">
        </footer>

    </div>{{!--/.post-card-content--}}

</article>
